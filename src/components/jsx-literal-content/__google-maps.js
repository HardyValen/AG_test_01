import { FontAwesomeIcon } from "@fortawesome/react-fontawesome"

const LC_LOCATION = () => {
  return (
    <span>
      <FontAwesomeIcon icon={['fas', 'map-marker-alt']}/>&ensp;CEK GOOGLE MAPS
    </span>
  )
}

export default LC_LOCATION