import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const LC_TELEPON = () => {
  return (
    <span>
      <FontAwesomeIcon icon={['fas', 'phone-alt']}/>&ensp;(022) 5204645
    </span>
  )
}

export default LC_TELEPON
